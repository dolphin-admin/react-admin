import type { AliasToken } from 'antd/es/theme/interface'

export const themeToken: Partial<AliasToken> = Object.freeze({
  fontFamily:
    'Nunito, Noto Sans SC, system-ui, -apple-system, Roboto, Helvetica Neue, Arial, sans-serif'
})
